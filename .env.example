# Application
APP_ENV=development
APP_LOG_LEVEL=INFO
APP_HOST=0.0.0.0
APP_PORT=8000

# Postgres (used in Phase 1+)
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=earning_edge
POSTGRES_USER=earning_edge
POSTGRES_PASSWORD=earning_edge_dev

# Redis (used in Phase 2+)
REDIS_HOST=redis
REDIS_PORT=6379

# Encryption master key (Fernet, base64-urlsafe 32 bytes) — generated in Phase 1
APP_ENCRYPTION_KEY=

# Telegram (Phase 2). Create a bot via @BotFather and paste the token here.
TELEGRAM_BOT_TOKEN=
# Optional Telegram tuning
# Long-polling is the default in dev. To switch to webhook, set TELEGRAM_USE_WEBHOOK=true
# and provide TELEGRAM_WEBHOOK_URL + TELEGRAM_WEBHOOK_SECRET.
TELEGRAM_USE_WEBHOOK=false
TELEGRAM_WEBHOOK_URL=
TELEGRAM_WEBHOOK_SECRET=

# LLM model aliases (Phase 7) — resolved to concrete OpenRouter IDs at runtime
MARKET_ANALYSIS_MODEL=claude-opus-4.7-thinking
LIGHTWEIGHT_MODEL=gemini-3.1-flash

# TradingView / browser automation (Phase 4)
TRADINGVIEW_EMAIL=
TRADINGVIEW_PASSWORD=
TRADINGVIEW_HEADLESS=false
TRADINGVIEW_TIMEOUT_MS=30000
TRADINGVIEW_STORAGE_STATE_PATH=var/tradingview/storage-state.json

# Backup earnings calendar (Phase 4)
FINNHUB_API_KEY=
